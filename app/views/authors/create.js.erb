<% if @author.persisted? %>
	$('.authors').append('<%= escape_javascript render('author', author: @author) %>');
<% else %>
	alert('ogarnij się');
	alert('<%= j @author.errors.full_messages.join(', ') %>');
<% end %>

$('.new-author').find('input[type="text"], input[type="number"]').val('');